# Copy to .env.local (gitignored) for local XAMPP / CLI migrations.
# Overrides DB_* from .env so migrate.php can reach a local database.

DB_HOST=localhost
DB_NAME=kareh_spa
DB_USER=root
DB_PASSWORD=
